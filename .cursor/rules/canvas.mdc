---
alwaysApply: true
---

前端项目工程化开发规范
一、规范总则
本规范基于前端工程化开发标准制定，统一项目代码编写、目录管理、组件开发、样式配置、接口管理规则，保障项目代码整洁、统一、可维护、可扩展。项目全局UI组件、图标统一使用 Ant Design 组件库，所有开发行为严格遵循本规范，全员强制执行。
核心原则：目录分层清晰、命名语义化、功能模块化、代码复用化、样式统一化、接口规范化。
二、项目核心目录规范
严格遵循指定目录划分，禁止随意创建、修改目录结构，各目录功能职责单一、互不冗余。
2.1 核心目录映射规则

- src/components/Canvas：专属存放所有 Canvas 画布相关自定义组件，统一聚合管理
- src/utils：存放全局通用工具函数、公共方法、数据处理逻辑
- src/assets/styles/global.css：项目全局样式、Ant Design 组件全局覆盖样式统一修改入口
- src/api：所有后端请求接口、接口枚举、请求拦截、响应处理统一存放
- src/components：存放非Canvas类全局通用业务组件、基础封装组件
  2.2 完整标准目录结构
  src
  ├── api # 接口请求层
  ├── assets # 静态资源
  │ └── styles
  │ └── global.css # 全局样式、Ant组件全局样式覆盖
  ├── components # 全局通用组件
  │ └── Canvas # Canvas专属组件目录（核心专项目录）
  │ ├── index.jsx # 统一导出入口文件
  │ ├── CanvasBoard # 画布核心组件
  │ ├── CanvasNode # 画布节点组件
  │ └── ... # 其他Canvas相关子组件
  ├── utils # 全局工具函数
  ├── router # 路由配置
  ├── store # 状态管理
  └── App.jsx
  三、组件开发规范
  3.1 通用组件规则

1. 技术栈统一：项目所有UI组件、图标、表单、弹窗、布局等基础能力，一律使用 Ant Design 官方组件，禁止自定义原生基础UI组件、禁止引入其他UI组件库。
2. 组件分层：通用业务组件放入src/components，页面私有组件写在对应pages页面目录内，所有Canvas相关组件必须统一放入 src/components/Canvas，不得分散存放。
3. 组件封装原则：对Ant组件的二次封装、业务定制化组件，需保证纯组件特性，单一职责，支持props传参、事件透传，具备复用性。
   3.2 Canvas专项组件规范（强制）
4. 目录归属：所有与画布、绘图、拖拽、节点、连线、画布渲染相关的组件、子组件，全部归类至 src/components/Canvas 文件夹，禁止放置在页面目录或其他components子目录。
5. 统一导出规则：Canvas目录下必须创建 index.jsx 统一入口文件，所有Canvas组件通过该文件集中导出，页面使用时统一从入口导入，禁止直接引入子组件文件。
6. 导出示例：
   // src/components/Canvas/index.jsx
   import CanvasBoard from './CanvasBoard'
   import CanvasNode from './CanvasNode'
   import CanvasLine from './CanvasLine'

export {
CanvasBoard,
CanvasNode,
CanvasLine
} 4. 使用示例：
import { CanvasBoard } from '@/components/Canvas'

3.3 组件命名规范

1. 文件/文件夹命名：采用 大驼峰（PascalCase），与组件名保持一致，例：CanvasBoard、UserModal、SearchForm。
2. 语义化命名：组件名称必须见名知意，精准描述组件功能，禁止拼音、缩写、无意义命名。

✅ 规范：CanvasBoard（画布主体）、CanvasToolBar（画布工具栏）

❌ 不规范：Canvs、Tb、画布组件

3. 页面组件：页面文件夹采用小驼峰，页面根组件采用大驼峰。
   四、函数与变量命名规范
   4.1 函数命名（强制语义化）
1. 统一使用 小驼峰（camelCase） 命名，所有函数、方法、钩子遵循该规则。
1. 函数名必须为 动词+名词 组合，精准表达功能逻辑，禁止模糊命名。

✅ 规范：getCanvasData（获取画布数据）、updateNodeInfo（更新节点信息）、handleCanvasDrag（处理画布拖拽）
❌ 不规范：data、change、handleClick、func1

3. 事件处理函数统一以 handle 开头，数据获取统一以 get 开头，数据更新统一以update 开头，删除逻辑统一以 delete 开头。
4. 工具函数纯逻辑解耦，无DOM依赖、无业务耦合，可全局复用。
   4.2 变量命名规范
5. 普通变量：小驼峰，语义化，例：canvasList、nodeConfig、formValue。
6. 常量变量：全大写+下划线分割，统一存放或单独常量文件，例：CANVAS_WIDTH、NODE_TYPE_ENUM。
   五、工具函数（utils）规范
   5.1 目录职责
   src/utils 存放全局通用纯工具方法，包含：数据格式化、正则校验、日期处理、画布通用计算、参数处理、防抖节流、深拷贝等通用逻辑，禁止存放业务专属逻辑、DOM渲染逻辑、接口请求逻辑。
   5.2 拆分规范
7. 按功能模块拆分文件，单一文件职责单一，例：

utils/format.js 数据格式化

utils/validate.js 表单/数据校验

utils/canvas.js 画布专属工具方法
utils/common.js 通用基础方法

2. 所有工具函数统一导出，支持全局引入复用，函数必须添加注释，说明入参、出参、功能。
3. 禁止在组件内编写重复通用工具逻辑，需统一抽离至utils。
   六、全局样式规范
   6.1 样式统一入口
   项目所有 Ant Design 全局样式覆盖、全局基础样式、重置样式、全局变量样式，统一在 src/assets/styles/global.css 文件中修改编写，禁止分散在各个组件内做全局样式覆盖。
   6.2 样式编写规则
4. Ant组件样式覆盖必须使用组件官方类名，权重统一，避免样式冲突、层级混乱。
5. 全局样式只编写公共重置、Ant主题覆盖、全局通用样式，页面私有样式、组件私有样式使用模块化样式编写。
6. 样式命名规范：统一使用短横线命名法（kebab-case），语义化命名。
   七、API接口规范
   7.1 目录职责
   src/api 为项目唯一接口请求目录，存放所有后端接口请求方法、接口地址配置、请求拦截、响应拦截、接口枚举，页面及组件禁止直接编写axios请求逻辑。
   7.2 接口拆分规范
7. 按业务模块拆分接口文件，例：canvasApi.js（画布相关接口）、userApi.js（用户相关接口）。
8. 所有接口函数语义化命名，以 req / get / post 区分请求方式，见名知意。

✅ 规范：reqCanvasList、updateCanvasConfig、deleteCanvasNode 3. 统一封装axios请求实例，统一处理请求头、超时、错误提示、响应拦截，全局接口统一拦截处理。4. 接口参数、返回数据复杂场景需添加注释说明，复杂参数建议定义类型（TS）或参数说明。
八、Ant组件&图标使用规范

1. 全局唯一UI库：项目所有UI能力、图标、弹窗、表单、表格、布局、提示等，全部使用Ant Design官方组件，禁止混用其他UI库。
2. 图标使用：统一使用Ant Design Icon图标，按需引入，禁止本地存放图标、自定义图片图标替代官方图标。
3. 组件二次封装：业务定制化需求基于Ant原生组件二次封装，保留原生组件所有属性、事件透传，不破坏原生组件特性。
4. 样式定制：Ant组件全局主题、默认样式修改，统一在 global.css 中全局覆盖，局部样式可在组件内单独编写。
   九、通用代码规范补充
5. 注释规范：所有自定义组件、工具函数、接口方法必须添加功能注释，复杂逻辑添加步骤注释。
6. 复用规范：重复代码必须抽离为组件或工具函数，杜绝代码冗余。
7. 引入规范：所有自定义组件、工具、接口统一使用别名路径引入，禁止相对层级过长引入。
8. 文件导出规范：组件默认导出，工具函数、接口统一批量导出，保证导入统一、整洁。
